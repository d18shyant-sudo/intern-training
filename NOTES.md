uv :
1.It is fast in download packages 
2.It manages the environment automatically
3.still need to able to download some packages
pip :
1. It is slow compare to uv
2.It need external and depended for managing environment
3.It has all packages for a download 

workflow:
uv :
1. uv venv 
2. cd .venv\\Scripts\\activate
3.uv pip install <package-name> or uv add >package-name>
pip :
1. cd <to the project folder>s
2.pip install <package-name>

