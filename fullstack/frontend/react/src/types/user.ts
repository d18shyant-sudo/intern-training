export type FormData = {
  name: string;
  email: string;
  dob: string;
};

export type User = {
  id:number
  name: string;
  email: string;
  DOB: string;
};

export type login={
  username:string;
  password:string;
}